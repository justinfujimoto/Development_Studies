39-switch statements: How to start them

switch() {
  case "Saturday"  :
    alert("Yay");
    break;
  default  :
    alert("sad");
}

first keyword:
switch

second keyword:
case

/*

	- More conditions to test, more you need a SWITCH statement
	
	- Wherever you could use an IF statement (and variations), you can use a SWITCH statement
	
	switch (age) {
	case 21 :
	}

	first line of a switch statement tests the variable population
	switch(population) {
	}
	
	// test if a string is a particular variable
		
		switch(var1) {       if (var1 === "Hello") {
		case "Hello" :       {
		}
	
	