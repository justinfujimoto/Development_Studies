17-Arrays: Removing and inserting elements

splice (non-array #, array #??)
slice (non-array #, non-array #)

splice(1-based, 1-based number of elements to be removed, add element(s));
	starting position, elements removed, elements added

slice(1-based number before slice, final number to be sliced);
