9-Prompts

var response = prompt("message", "default")

var response = prompt(varMessage, defaultString);

/*
	- Similar to an alert
	- User input & response field
	- prompt(message, default answer)
	
	- If use - response as-is - default response is assigned to variable
	- Assign strings to var THEN specify variables

	Steps
	1. Question
	2. Default
	3. Prompt(Question, default);


	- All response come back as strings

	- IF the user enters nothing - clicks OK
	- var is assigned an empty "" string

	- IF the user enters - cancel 
	- Assigned value is null

	Two forms to write prompt
	- prompt
	- windows.prompt

*/


